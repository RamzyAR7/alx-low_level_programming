#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 * by ramzy
 * @a : get the sum of them
 * @b : get the sum of them
 * Return: Always 0
*/
int add(int a, int b)
{
	return (a + b);
}
