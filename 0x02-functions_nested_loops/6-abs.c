#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer.
 * @number : the character to check
 * by ramzy
 * Return: 0
*/
int _abs(int number)
{
	if (number < 0)
	{
		return (number * (-1));
	}
	else
	{
		return (number);
	}
}
