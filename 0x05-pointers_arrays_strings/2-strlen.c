#include "main.h"
/**
 * _strlen - a function that returns the length of a string.
 * @s: char of pointer
 * Return: Always length
 * by ramzy
*/
int _strlen(char *s)
{
	int length = sizeof(*s);

	return (length);
}
