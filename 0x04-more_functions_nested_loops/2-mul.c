#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: the first number
 * @b: the second number
 * Return: Always 0 (success)
*/
int mul(int a, int b)
{
	int mul_result = a * b;

	return (mul_result);
}
